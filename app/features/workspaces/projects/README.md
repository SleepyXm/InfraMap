# Projects

Owns the unified InfraMap project model that attaches repositories, deployments, databases, hosted backends, domains, and environments into one manageable project. The API and hook persist the workspace project shell before provider resources are attached; `ProjectWorkspace` provides the creation and listing surface.
